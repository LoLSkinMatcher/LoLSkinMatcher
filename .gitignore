__pycache__/
build/
dist/
*.spec
*.old.exe
*.old2.exe
.claude/
# real Firebase config (copy from the .example files)
firebase_config.json
.firebase/
# personal, local-only tools (account switcher, etc.) — never shared
personal/
